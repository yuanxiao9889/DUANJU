## 新增

- 新增 Windows 安装包发布构建流程，发布 tag 后自动上传 Windows 与 macOS 安装产物。

## 优化

- 优化 4K 级别大图生成与传输链路的超时配置，降低长耗时任务被提前中断的概率。
- 更新应用图标与标题栏品牌资源，保持安装包与应用内视觉一致。

## 修复

- 修复 Qwen3 TTS Complete 在 Windows 下启动时，本地模型路径被 HuggingFace 误判为远端仓库路径，导致 `speech_tokenizer/config.json` 检查失败的问题。
