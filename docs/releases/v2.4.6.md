## 优化

- 自动数据库备份增加数量与容量裁剪，保留最近自动备份并限制自动备份总量，避免长期使用后备份目录无限增长。
- 数据库备份列表与存储信息读取时会自动清理旧的临时备份和 SQLite 旁路文件，降低存储维护成本。

## 修复

- 修复项目保存前频繁生成 `pre_persist` 整库备份的问题，避免大体积 `projects.db` 被反复复制导致磁盘占用暴涨和应用卡顿。
- 修复备份目录中 `.db-journal`、`.db-wal`、`.db-shm` 残留文件未清理的问题。
