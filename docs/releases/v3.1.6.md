## 优化

- 新增画布连接线显示开关，概览视图下浏览大画布更清爽。
- 清理已停用的 Azemm 渠道入口与模型列表，减少无效配置项干扰。

## 修复

- 修复 OOPII `monkey-pro`、`monkey-2` 改名后 Gemini 图片请求未正确携带尺寸参数，选择 `4K` 仍可能返回 `1K` 图片的问题。
- 同步补充 `monkey-image-*` 路由识别与回归测试，避免模型别名调整后再次出现分辨率回落。
