## 优化

- 将画布图片预览默认档位从最长边 `512px` 调整为 `256px`，降低大画布场景下的媒体加载压力。
- 为 Markdown 渲染增加旧版 Safari / WebKit 兼容降级，避免因不支持的正则特性影响界面启动。

## 修复

- 修复部分 Mac 设备启动应用时，因旧版 WebKit 不支持后行断言与 Unicode 正则属性导致的黑屏报错。
- 修复剧本分镜表节点里“生成质量”等模型额外参数无法保存，并且不能同步到分镜生产出图节点的问题。
