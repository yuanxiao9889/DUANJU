## 优化

- 优化分镜生产编组的连续参考链，下一组生成时优先使用上一组已选中的分镜结果作为参考。
- 优化分镜生产编组的实际时长显示与视频节点时长配置，统一按组合镜头的合计时长计算。
- 优化分镜生产结果占位节点，支持结果列表删除、选中参考图保留和空结果状态清理。
- 优化分镜生产提示词的素材引用去重，避免同时出现 `@角色名` 与 `@角色角色名` 这类重复引用。

## 修复

- 修复清空分镜生产结果后，旧生成任务或旧预览图再次回填的问题。
- 修复分镜生产编组刷新时可能丢失已有图片结果的问题。
